<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.MaterialOrderMapper">

	<!-- 1. 발주 목록 조회 -->
	<select id="selectOrderList" parameterType="com.itwillbs.domain.SearchCriteria" resultType="com.itwillbs.domain.MaterialOrderVO">
		SELECT 
			mo.order_id AS orderId,
			mo.supplier_id AS supplierId,
			s.supplier_name AS supplierName, 
			mo.order_date AS orderDate,
			mo.order_status AS orderStatus,
			mo.expected_arrived_date AS expectedArrivedDate,
			mo.created_by AS createdBy,
			mo.note,
			<!-- 대표 품명: 자재가 1건이면 그대로, 2건 이상이면 "외 N건" -->
			CASE 
				WHEN COUNT(moi.material_id) = 1 THEN MAX(m.material_name)
				ELSE CONCAT(MAX(m.material_name), ' 외 ', COUNT(moi.material_id) - 1, '건')
			END AS materialName,
			SUM(moi.order_quantity) AS totalQuantity,
			SUM(moi.total_price) AS totalPrice,
			MAX(moi.warehouse_code) AS warehouseCode
		FROM material_order mo
		JOIN material_order_item moi ON mo.order_id = moi.order_id
		JOIN material m ON moi.material_id = m.material_id
		JOIN supplier s ON mo.supplier_id = s.supplier_id 
		<where>
			<!-- 검색 조건 -->
			<if test="keyword != null and keyword != ''">
				(mo.order_id LIKE CONCAT('%', #{keyword}, '%')
				OR m.material_name LIKE CONCAT('%', #{keyword}, '%')
				OR s.supplier_name LIKE CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="startDate != null and startDate != ''">
				AND mo.order_date &gt;= #{startDate}
			</if>
			<if test="endDate != null and endDate != ''">
				AND mo.order_date &lt;= #{endDate}
			</if>
			<if test="status != null and status != ''">
				AND mo.order_status = #{status}
			</if>
		</where>
		GROUP BY mo.order_id, mo.supplier_id, s.supplier_name, mo.order_date, 
				 mo.order_status, mo.expected_arrived_date, mo.created_by, mo.note
		
		<choose>
			<when test="sortColumn == 'order_id'">
				ORDER BY
					CASE mo.order_status
						WHEN '요청' THEN 1
						WHEN '승인' THEN 2
						WHEN '입고완료' THEN 3
						ELSE 4
					END,
					mo.order_id ${sortOrder}
			</when>
			<when test="sortColumn == 'order_date'">
				ORDER BY
					CASE mo.order_status
						WHEN '요청' THEN 1
						WHEN '승인' THEN 2
						WHEN '입고완료' THEN 3
						ELSE 4
					END,
					mo.order_date ${sortOrder}
			</when>
			<when test="sortColumn == 'expected_arrived_date'">
				ORDER BY
					CASE mo.order_status
						WHEN '요청' THEN 1
						WHEN '승인' THEN 2
						WHEN '입고완료' THEN 3
						ELSE 4
					END,
					mo.expected_arrived_date ${sortOrder}
			</when>
			<otherwise>
				ORDER BY
					CASE mo.order_status
						WHEN '요청' THEN 1
						WHEN '승인' THEN 2
						WHEN '입고완료' THEN 3
						ELSE 4
					END,
					mo.order_date DESC
			</otherwise>
		</choose>

		LIMIT #{pageStart}, #{perPageNum}
	</select>
	
	<!-- 2. 총 건수 조회 -->
	<select id="getTotalCount" parameterType="com.itwillbs.domain.SearchCriteria" resultType="int">
		SELECT COUNT(DISTINCT mo.order_id)
		FROM material_order mo
		JOIN material_order_item moi ON mo.order_id = moi.order_id
		JOIN material m ON moi.material_id = m.material_id
		JOIN supplier s ON mo.supplier_id = s.supplier_id
		<where>
			<if test="keyword != null and keyword != ''">
				(mo.order_id LIKE CONCAT('%', #{keyword}, '%')
				OR m.material_name LIKE CONCAT('%', #{keyword}, '%')
				OR s.supplier_name LIKE CONCAT('%', #{keyword}, '%'))
			</if>
			<if test="startDate != null and startDate != ''">
				AND mo.order_date &gt;= #{startDate}
			</if>
			<if test="endDate != null and endDate != ''">
				AND mo.order_date &lt;= #{endDate}
			</if>
			<if test="status != null and status != ''">
				AND mo.order_status = #{status}
			</if>
		</where>
	</select>
	
	<!-- 발주번호 생성 -->
	<select id="generateOrderId" resultType="String">
		SELECT CONCAT(
			'PO-RM-', 
			DATE_FORMAT(NOW(), '%Y%m%d'), 
			'-', 
			LPAD(
				IFNULL(MAX(CAST(SUBSTRING(order_id, 16, 3) AS UNSIGNED)), 0) + 1, 
				3, 
				'0'
			)
		)
		FROM material_order
		WHERE order_id LIKE CONCAT('PO-RM-', DATE_FORMAT(NOW(), '%Y%m%d'), '-%')
	</select>
	
	<!-- 발주 등록 -->
	<insert id="insertOrder" parameterType="com.itwillbs.domain.MaterialOrderVO">
		INSERT INTO material_order (
			order_id, supplier_id, order_date, order_status,
			expected_arrived_date, created_by, note, work_order_id
		) VALUES (
			#{orderId}, 
			#{supplierId}, 
			#{orderDate, jdbcType=DATE}, 
			#{orderStatus},
			#{expectedArrivedDate, jdbcType=DATE}, 
			#{createdBy}, 
			#{note},
			#{workOrderId}
		)
	</insert>
	
	<!-- 발주 항목 등록 (VO 타입) -->
	<insert id="insertOrderItem" parameterType="com.itwillbs.domain.MaterialOrderItemVO">
		INSERT INTO material_order_item (
			order_item_id, order_id, material_id, work_order_id, order_quantity,
			unit_price, total_price, warehouse_code
		) VALUES (
			#{orderItemId},
			#{orderId}, 
			#{materialId}, 
			#{workOrderId},
			#{orderQuantity, jdbcType=INTEGER},
			#{unitPrice, jdbcType=INTEGER},
			#{totalPrice, jdbcType=INTEGER}, 
			#{warehouseCode}
		)
	</insert>
	
	<!-- 발주 항목 등록 (Map 타입) -->
	<insert id="insertOrderItemMap" parameterType="map">
		INSERT INTO material_order_item (
			order_item_id, order_id, material_id, work_order_id, order_quantity,
			unit_price, total_price, warehouse_code
		) VALUES (
			#{orderItemId},
			#{orderId}, 
			#{materialId}, 
			#{workOrderId},
			#{orderQuantity, jdbcType=INTEGER},
			#{unitPrice, jdbcType=INTEGER},
			#{totalPrice, jdbcType=INTEGER}, 
			#{warehouseCode}
		)
	</insert>
	
	<!-- 자재명 키워드로 거래처 + 자재 단가 정보 조회 -->
	<select id="searchSuppliersByMaterial" parameterType="string" resultType="com.itwillbs.dto.SupplierItemDTO">
		SELECT
			m.material_name AS materialName,
			s.supplier_id AS supplierId,
			s.supplier_name AS supplierName,
			si.unit_price AS unitPrice,
			m.warehouse_code AS warehouseCode
		FROM supplier_item si
		JOIN material m ON si.material_id = m.material_id
		JOIN supplier s ON si.supplier_id = s.supplier_id
		WHERE m.material_name LIKE CONCAT('%', #{keyword}, '%')
			AND si.supply_available = 'Y'
			AND m.use_yn = 'Y'
			AND m.purchasable_flag = 'Y'
		ORDER BY s.supplier_name ASC
	</select>
	
	<!-- 부족분 발주 자동 생성 관련 쿼리 -->
	
	<!-- 자재ID 목록으로 거래처-자재 매핑 정보 조회 -->
	<select id="selectSupplierItemMappings" parameterType="list" resultType="map">
		SELECT
			si.supplier_id AS supplierId,
			si.material_id AS materialId,
			si.unit_price AS unitPrice,
			COALESCE(m.warehouse_code, 'WH001') AS warehouseCode,
			s.supplier_name AS supplierName,
			m.material_name AS materialName
		FROM supplier_item si
		JOIN material m ON m.material_id = si.material_id
		JOIN supplier s ON s.supplier_id = si.supplier_id
		WHERE si.supply_available = 'Y'
			AND m.use_yn = 'Y'
			AND m.purchasable_flag = 'Y'
			AND si.material_id IN
			<foreach collection="list" item="materialId" open="(" separator="," close=")">
				#{materialId}
			</foreach>
		ORDER BY si.supplier_id, m.material_name
	</select>
	
	<!-- 발주 초안 헤더 등록 -->
	<insert id="insertOrderHeaderDraft" parameterType="map">
		<selectKey keyProperty="orderId" resultType="string" order="BEFORE">
			SELECT CONCAT(
				'PO-RM-',
				DATE_FORMAT(NOW(), '%Y%m%d'),
				'-',
				LPAD(
					IFNULL(MAX(CAST(SUBSTRING(order_id, 16, 3) AS UNSIGNED)), 0) + 1,
					3, 
					'0'
				)
			)
			FROM material_order
			WHERE order_id LIKE CONCAT('PO-RM-', DATE_FORMAT(NOW(), '%Y%m%d'), '-%')
		</selectKey>
		
		INSERT INTO material_order (
			order_id, supplier_id, order_date, order_status,
			expected_arrived_date, created_by, note, work_order_id
		) VALUES (
			#{orderId}, 
			#{supplierId}, 
			NOW(), 
			COALESCE(#{orderStatus}, '초안'),
			#{expectedArrivedDate, jdbcType=DATE}, 
			#{createdBy}, 
			#{note},
			#{workOrderId}
		)
	</insert>
	
	<!-- 발주 항목의 다음 순번 조회 -->
	<select id="selectNextOrderItemIndex" parameterType="string" resultType="int">
		SELECT COALESCE(
			MAX(CAST(SUBSTRING_INDEX(order_item_id, '-', -1) AS UNSIGNED)), 
			0
		) + 1
		FROM material_order_item
		WHERE order_id = #{orderId}
	</select>
	
	<!-- 발주 항목 배치 등록 -->
	<insert id="insertOrderItemsBatch" parameterType="list">
		<if test="list != null and list.size() > 0">
			INSERT INTO material_order_item (
				order_item_id, order_id, material_id, work_order_id, order_quantity,
				unit_price, total_price, warehouse_code
			) VALUES
			<foreach collection="list" item="item" separator=",">
				(
					#{item.orderItemId},
					#{item.orderId},
					#{item.materialId},
					#{item.workOrderId},
					#{item.orderQuantity, jdbcType=INTEGER},
					#{item.unitPrice, jdbcType=INTEGER},
					#{item.totalPrice, jdbcType=INTEGER},
					#{item.warehouseCode}
				)
			</foreach>
		</if>
	</insert>

	<!-- 내부사용(N) 자재 조회 -->
	<select id="selectNonPurchasableFromList" parameterType="list" resultType="map">
	  SELECT m.material_id AS materialId, m.material_name AS materialName
	  FROM material m
	  WHERE m.purchasable_flag = 'N'
	    AND m.material_id IN
	    <foreach collection="list" item="id" open="(" separator="," close=")">
	      #{id}
	    </foreach>
	</select>
	
	
<!-- ===================================================================== -->
<!-- 발주 초안에서 요청 쿼리 -->	
<!-- ===================================================================== -->

	<!-- 0) 초안 + 항목 로드 (검증용) -->
	<select id="selectDraftWithItems" parameterType="string" resultType="map">
	  SELECT 
	    mo.order_id       AS orderId,
	    mo.order_status   AS orderStatus,
	    mo.supplier_id    AS supplierId,
	    mo.order_date     AS orderDate,
	    mo.expected_arrived_date AS expectedArrivedDate,
	    mo.created_by     AS createdBy,
	    mo.note,
	    moi.order_item_id AS orderItemId,
	    moi.material_id   AS materialId,
	    moi.order_quantity AS orderQuantity,
	    moi.unit_price    AS unitPrice,
	    moi.total_price   AS totalPrice,
	    moi.warehouse_code AS warehouseCode,
	    moi.work_order_id AS workOrderId
	  FROM material_order mo
	  JOIN material_order_item moi ON moi.order_id = mo.order_id
	  WHERE mo.order_id = #{orderId}
	</select>
	
	<!-- 0-1) 항목 총액 합계 (있으면 보여주기/로그용) -->
	<select id="selectItemsTotal" parameterType="string" resultType="int">
	  SELECT COALESCE(SUM(total_price),0)
	  FROM material_order_item
	  WHERE order_id = #{orderId}
	</select>
	
	<!-- 1) 상태 전이: 초안 -> 요청 (동시성 보호: 초안일 때만) -->
	<update id="updateOrderToRequested" parameterType="string">
	  UPDATE material_order
	     SET order_status = '요청'
	   WHERE order_id = #{orderId}
	     AND order_status = '초안'
	</update>



<!-- ===================================================================== -->
<!-- 발주 상세 -->	
<!-- ===================================================================== -->
	<!-- A. 주문 헤더 -->
	<select id="selectOrderHeader" parameterType="string" resultType="map">
	  SELECT 
	    mo.order_id               AS orderId,
	    mo.supplier_id            AS supplierId,
	    s.supplier_name           AS supplierName,
	    mo.order_date             AS orderDate,
	    mo.expected_arrived_date  AS expectedArrivedDate,
	    mo.order_status           AS orderStatus,
	    mo.created_by             AS createdBy,
	    mo.note                   AS note,
	    mo.work_order_id 		  AS workOrderId
	  FROM material_order mo
	  JOIN supplier s ON s.supplier_id = mo.supplier_id
	  WHERE mo.order_id = #{orderId}
	</select>
	
	<!-- B. 주문 아이템 목록 -->
	<select id="selectOrderItems" parameterType="string" resultType="map">
	  SELECT
	    moi.order_item_id   AS orderItemId,
	    moi.material_id     AS materialId,
	    m.material_name     AS materialName,
	    moi.order_quantity  AS orderQuantity,
	    moi.unit_price      AS unitPrice,
	    moi.total_price     AS totalPrice,
	    moi.warehouse_code  AS warehouseCode,
	    moi.work_order_id   AS workOrderId
	  FROM material_order_item moi
	  JOIN material m ON m.material_id = moi.material_id
	  WHERE moi.order_id = #{orderId}
	  ORDER BY moi.order_item_id
	</select>





<!-- ===================================================================== -->
<!-- 발주 상태별 탭기능 카운트 -->	
<!-- ===================================================================== -->
	<select id="getCountByStatus" parameterType="string" resultType="int">
	    SELECT COUNT(*) 
	    FROM material_order 
	    WHERE order_status = #{status}
	</select>
	
	<!-- 또는 한 번에 모든 상태 카운트 조회 -->
	<select id="getStatusCounts" resultType="map">
	    SELECT order_status as status, COUNT(*) as count
	    FROM material_order 
	    GROUP BY order_status
	</select>


		<!-- 메일 승인 -->
		<select id="findById" parameterType="string" resultType="com.itwillbs.domain.MaterialOrderVO">
		  SELECT * FROM material_order WHERE order_id = #{orderId}
		</select>
		
		<!-- 협력사 발주 승인 상태 변경 -->
		<update id="updateOrderStatus" parameterType="com.itwillbs.domain.MaterialOrderVO">
		  UPDATE material_order
		  SET order_status = #{orderStatus}
		  WHERE order_id = #{orderId}
		</update>




</mapper>